// non-runnable code 

// procedural
processamento(valor1, valor2, valor3)
// passando valores para que um procedimento use-os
// função que manipula dados


// OO
obj = {
    valor1,
    valor2,
    valor3,
    processamento(){
        // ...
    }
}
// agrupa comportamento e atributos
// algo mais relacionado ao mundo real
// foco no objeto

/* Principios importantes:

1. abstração: atributos e funções de um mesmo objeto da vida real pode variar de
acordo com o contexto.

2. encapsulamento: informações essenciais mas que não são pertinentes para o usuario ficam
encapsuladas(escondidas). Diminui a dependência de outros objetos

// 3. herança (prototype) - reuso de código - um objeto que recebe atributos e comportamentos
de um objeto pai, é aconselhável valorizer mais a composição do que a herença 

4. polimorfismo - a parir de um obejeto pai é possível criar objetos mais concretos, o termo
polimorfismo é atribuído à essa carecterisca.
*/
